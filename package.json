{
  "name": "ph-ereferral-connectathon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "R12 PH eReferral Connectathon demo using Express, TypeScript, and FHIR R4",
  "scripts": {
    "dev": "npm run build && tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.client.json && cp src/public/index.html src/public/styles.css src/public/incoming.css dist/public/",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.client.json --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
